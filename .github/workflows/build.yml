
name: Security Gates

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - '*'
  workflow_dispatch:

jobs:
  security-gates:
    uses: Trendyol/security-actions/.github/workflows/security-gates.yml@master
    with:
      needs: build
    permissions:
      actions: read
      contents: read
      security-events: write
